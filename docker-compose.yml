version: "3.8"

services:
  web:
    build: .
    image: sandboxai-nextjs:latest
    container_name: sandboxai-nextjs
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
